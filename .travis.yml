language: java
jdk:
  - openjdk8
cache:
  directories:
    - $HOME/.m2
install: echo "Skipping pre-fetch of maven dependencies"
script: cp .travis.settings.xml $HOME/.m2/settings.xml && mvn clean package
